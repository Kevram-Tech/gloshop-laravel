# Application Configuration
APP_NAME=GloShop
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=http://72.60.188.146

# Database Configuration
# For docker-compose.yml (with MySQL service): use DB_HOST=mysql
# For external MySQL: use your MySQL server IP/hostname
DB_CONNECTION=mysql
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=gloshop
DB_USERNAME=gloshop-user
DB_PASSWORD=Alkashi13@@#

# Cache Configuration
CACHE_DRIVER=file
SESSION_DRIVER=file
QUEUE_CONNECTION=sync

# Mail Configuration
MAIL_MAILER=smtp
MAIL_HOST=mailhog
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="noreply@72.60.188.146"
MAIL_FROM_NAME="${APP_NAME}"

# Redis Configuration (optional)
REDIS_HOST=redis
REDIS_PASSWORD=null
REDIS_PORT=6379

# Logging
LOG_CHANNEL=stack
LOG_LEVEL=error

# Session
SESSION_LIFETIME=120

# Timezone
APP_TIMEZONE=UTC

