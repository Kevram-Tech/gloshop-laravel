-- Initialization script for MySQL database
-- This script runs only when the database is first created

-- Set timezone
SET time_zone = '+00:00';

-- Create database if it doesn't exist (usually already created by MYSQL_DATABASE env var)
-- CREATE DATABASE IF NOT EXISTS gloshop CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- Use the database
-- USE gloshop;

-- You can add any initial SQL commands here if needed
-- For example, creating additional users or setting up initial data

